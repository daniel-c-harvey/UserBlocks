@inject NavigationManager Navigation

@code {
    [Parameter]
    public required string? LoginPath { get; set; }
    
    protected override void OnInitialized()
    {
        Navigation.NavigateTo($"{LoginPath ?? "Identity/Account/Login"}?returnUrl={Uri.EscapeDataString(Navigation.Uri)}", true);
    }
}